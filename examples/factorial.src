fn factorial(int n) -> int {
    int result = 1;
    while (n > 1) {
        result = result * n;
        n = n - 1;
    }
    return result;
}

fn main() -> int {
    int x = 5;
    int fact = factorial(x);
    return fact;
}

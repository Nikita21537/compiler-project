// This file contains type errors that should be caught by semantic analyzer
fn main() -> int {
    int x = 10;

    // Error: condition must be boolean
    // if (x) {
    //     x = 1;
    // }

    // Error: while condition must be boolean
    // while (x) {
    //     x = x - 1;
    // }

    // Error: for condition must be boolean
    // for (int i = 0; x; i = i + 1) {
    //     x = x - 1;
    // }

    // Error: cannot assign to literal
    // 5 = x;

    // Error: type mismatch in assignment
    // x = true;

    // Error: undeclared variable
    // y = 10;

    // Error: duplicate declaration
    // int x = 20;

    return 0;
}